---
import "../styles/serviceCards.css"
const { image, alt, title, description } = Astro.props;
---

<!-- Card.astro -->

<div class="card">
  <img src={image} alt={alt} />
  <h1>{title}</h1>
  <p>{description}</p>
</div>
